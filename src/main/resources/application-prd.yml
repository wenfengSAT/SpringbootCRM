logging:
  level:
    root: info
server:
  port: 8080
  servlet:
    context-path: /
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    druid:
      initial-size: 1
      max-active: 20
      min-idle: 1
      stat-view-servlet:
        allow: true
      test-on-borrow: true
    password: 123456
    url: jdbc:mysql://localhost:3306/springboot_demo
    username: root
  thymeleaf:
    cache: true